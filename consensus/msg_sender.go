package consensus

import "github.com/patrickmao1/beeftea/types"

// Signs the msg and wraps it into types.Envelope and send to all peers
func (s *Service) broadcast(msg *types.Message) error {
	return nil
}
